let F1 = 0 
let F2 = 1 
let aux = 0 
let pertence = false

const S = 34 // Numero que deve ser verificado se pertence a sequencia de Fibonacci

if((S===F1)||(S===F2)){
  console.log ("Este numero pertence a sequencia de Fibonacci")
  }else{
  while(S >= aux){
  aux = F1 + F2
  F1 = F2
  F2 = aux
    if(S===aux){
      pertence=true
      break
    }
    } 
  if(pertence){
    console.log ("Este numero pertence a sequencia de Fibonacci")
  }else{
    console.log ("Este numero NÃO pertence a sequencia de Fibonacci")
  }
}
